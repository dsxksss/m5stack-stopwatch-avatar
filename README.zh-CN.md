# KK — M5Stack StopWatch 表情角色

[English](README.md) | [简体中文](README.zh-CN.md)

[![构建固件](https://github.com/dsxksss/m5stack-stopwatch-avatar/actions/workflows/build.yml/badge.svg)](https://github.com/dsxksss/m5stack-stopwatch-avatar/actions/workflows/build.yml)
[![许可证：AGPL v3](https://img.shields.io/badge/License-AGPL_v3-blue.svg)](LICENSE)

认识一下 **KK**：一个住在 M5Stack StopWatch 里的小表情角色。

KK 运行在 M5Stack StopWatch 的圆形 AMOLED 屏幕上。它的眼睛、眼皮、眉线、关键帧和过渡均由 C++ 实时绘制，不使用图片序列帧。产品画面保持纯黑背景，并针对 466 × 466 圆屏、局部刷新和直接交互进行了优化。

> 这是一个社区项目，与 M5Stack 官方没有隶属或背书关系。

## 当前固件

当前固件版本为 **0.12.1**。阅读模式包含内置书架和公网 HTTPS 书源；《零灯》已经拆分为 42 个合规阅读单元，章节、内容块和页码会保存在 NVS 中。章节与正文现在统一为双向操作：A 往回，B 往后。

0.12.0 固件和 SPIFFS 书库镜像已完成编译、烧录并在真实 M5Stack StopWatch 启动。真机识别到 42 个阅读单元，打开第一章后生成 22 页，在第 12 页退出后再次打开会直接恢复到 `12/22`。阅读与正常表情稳定窗口仍接近 60 fps，TE 同步 100%，没有帧超时。0.12.1 已通过完整 PlatformIO 构建；由于本次设备不在电脑旁，新增的上一页、上一内容块和上一章节实体按键路径仍需回家后验收。

## 主要特点

- 11 种程序化表情：`idle`、`listening`、`thinking`、`happy`、`excited`、`curious`、`confused`、`angry`、`surprised`、`sad`、`sleepy`；
- 通过 GPIO 38 的 CO5300 TE 信号进行硬件垂直同步，以约 60 fps 渲染，并使用动态脏矩形减少 AMOLED 传输开销；
- 支持单击、双击、长按、连续触摸跟随和上下/左右滑动；
- 加速度计与陀螺仪共同驱动倾斜跟随，眼睛先移动、头部稍后跟随，并受中心安全区限制；
- 快速移动设备会产生短暂惯性反馈，但不会切换当前表情；
- 表情由触摸、动作、电量、充电和网络等情境自动驱动，不提供手动表情目录；
- 表情、切换、电量状态、亮度和唤醒均配有原创的柔和 pop、boop、blip 合成音效，安静时段自动静音；
- 同时双击 A+B，在左右眼内竖排显示隐藏生日祝福；长按 A+B 进入硬件诊断；
- 长篇 UTF-8 内容使用独立的全屏六行分页、Unicode 打字机效果，并通过程序化过渡恢复原表情；
- 长按 B 打开独立模式菜单；阅读模式可选择无需联网的内置书架或公网 HTTPS 书源；
- 内置《零灯》共 42 个阅读单元，支持自动进入下一章，并保存章节、内容块和页码断点；
- 长按 A 呼出完全由眼睛组成的极简菜单；菜单内单击 A 浏览亮度、音量、动作灵敏度、夜静、网络和固件版本，双击 A 确认，B 返回。普通界面短按 A/B 均不切换表情；向下滑动直接查看电量，设置档位由眼睛睁开幅度表达；
- 45 秒无操作后自动变暗并降低渲染频率，60 秒后清黑并关闭 AMOLED，触摸、按键或明显移动可唤醒；
- 亮度、动作灵敏度、休眠时长和安静时段保存在设备 NVS 中；
- 最多保存 5 个 2.4 GHz Wi-Fi，管理页可添加、更新和删除，设备自动选择当前信号最强的已保存网络；
- Wi-Fi 连接后通过 NTP 自动同步中国标准时间并写入 RX8130 RTC，RTC 驱动夜间困倦和安静时段；
- 串口语义命令为未来语音识别或外部控制提供稳定入口。

## 完整交互说明

屏幕手势与 A/B 实体按键承担不同功能。普通表情界面短按 A 或 B 会刻意保持无动作，避免误切换表情或误操作已经保存的网络。

### 普通表情与屏幕手势

| 操作 | 结果 |
| --- | --- |
| 单击屏幕 | 短暂显示 `happy` |
| 双击屏幕 | 短暂显示 `surprised` |
| 原地按住屏幕约 650 ms | 短暂显示 `angry` |
| 按住但未越过滑动判定范围 | 眼睛和头部跟随触点 |
| 向右滑动 | 短暂显示 `curious` |
| 向左滑动 | 短暂显示 `confused` |
| 向上滑动 | 短暂显示 `surprised` |
| 向下滑动 | 打开沉浸式电量表情 |
| 缓慢倾斜设备 | 视线在受限的中心安全区内跟随倾斜方向 |
| 快速移动设备 | 眼睛和头部产生短暂惯性，但不改变当前表情 |

`idle`、`listening` 和 `thinking` 是可持续停留的基础状态。点击、长按、滑动、充电和低电量触发的反应都是临时状态，结束后会自动恢复之前的基础表情。

### 实体按键与各模式

| 当前模式 | A 键 | B 键 | A+B |
| --- | --- | --- | --- |
| 普通表情 | 长按约 800 ms 打开眼睛菜单；短按无动作 | 长按约 800 ms 打开模式菜单；短按无动作 | 同时双击竖排显示生日祝福；同时长按约一秒进入诊断模式 |
| 阅读来源菜单 | 单击切换内置书架/公网书源；双击进入 | 关闭菜单并恢复之前的表情 | 同时长按约一秒进入诊断模式 |
| 内置章节菜单 | 单击下一章；双击阅读或继续 | 单击上一章；长按返回阅读来源 | — |
| 公网书源页 | 更新/阅读已配置书源，或等待局域网投送 | 返回阅读来源；长按关闭 | — |
| 眼睛根菜单 | 单击切到下一项；双击打开当前项 | 关闭菜单并恢复之前的表情 | — |
| 亮度页 | 单击循环四档亮度 | 返回根菜单 | — |
| 音量页 | 单击循环静音与四档音量 | 返回根菜单 | — |
| 灵敏页 | 单击循环低、中、高三档动作灵敏度 | 返回根菜单 | — |
| 夜静页 | 单击开关定时静音 | 返回根菜单 | — |
| 网络页 | 长按约 1.8 秒配网、重试或换网；短按只刷新状态 | 取消正在运行的配网页面并返回根菜单 | — |
| 版本页 | 只读；单击只产生轻微震动反馈 | 返回根菜单 | — |
| 电量页 | 不修改设置 | 提前关闭 | — |
| 全屏文字页 | 上一页或上一内容块 | 补完/下一页或下一内容块；长按退出 | 同时长按约一秒进入诊断模式 |
| 公网图片页 | 上一内容块 | 下一内容块；长按退出 | — |
| 诊断模式 | 测试震动 | 重绘诊断画面 | 同时长按约一秒回到表情界面 |

根菜单中的单击 A 会在 420 ms 双击判定窗口结束后才切换项目；如果窗口内再次点击，则直接打开当前项目，不会先跳到下一项。眼睛菜单中仍可触摸屏幕让视线跟随，但触摸不会修改设置。

### 六项眼睛菜单

菜单始终保持 KK 的表情形态：项目名称显示在左眼，当前值或位置显示在右眼。

1. **亮度（`1/6`）**：A 在 `60`、`100`、`150`、`220` 四档之间循环；右眼睁开幅度表示当前 `1/4–4/4` 档位。
2. **音量（`2/6`）**：A 在 `0/4–4/4` 之间循环，`0/4` 为静音；右眼睁开幅度表示音量，非静音档位会播放短促试听音效。
3. **灵敏（`3/6`）**：A 在低、中、高三档动作灵敏度之间循环，右眼分别显示“低”“中”“高”。该设置同时调整倾斜和快速移动响应；为避免误唤醒，休眠状态的动作唤醒阈值保持不变。
4. **夜静（`4/6`）**：A 开关并保存 `22:00–07:00` 定时静音。该功能默认关闭且只控制声音，不影响 RTC 驱动的夜间困倦表情。
5. **网络（`5/6`）**：显示未连、连接、已连或失败。继续按住 A 约 1.8 秒启动临时热点 `KK-XXXX`，密码是全小写 `kkfriend`，双眼显示为 `kkfr | iend`。门户会列出已保存网络，可添加/更新 SSID 和密码，也可逐项删除；最多保存 5 项。候选网络只有连接成功后才写入设备，设备平时扫描可见的已保存项并连接信号最强者。密码不会回显到网页或串口。
6. **版本（`6/6`）**：显示当前固件版本 `0.12.1`，不会修改任何设置。

### 电量、自动反应与屏幕休眠

- 向下滑动会显示与电量相符的表情：左眼显示充电状态或简短能量描述，右眼显示实测电量百分比，眼睛睁开幅度也表示剩余电量。
- 约 3.4 秒后，电量文字通过眨眼和摇头动画退出，再过约 0.9 秒恢复居中的原基础表情；单击屏幕或按 B 可以提前关闭。
- 接入电源会触发 `excited`，拔掉电源会触发 `curious`；未充电且电量不高于 15% 时，会定期出现 `sleepy` 提醒。
- 无操作 45 秒后，屏幕降至最高 `24/255` 亮度并把渲染降低到 20 fps；60 秒后会先清黑，再彻底关闭 AMOLED。
- 触摸、任一实体按键或确认过的设备动作都可以唤醒屏幕；眼睛菜单、电量页和诊断模式在使用期间保持唤醒。
- 在设定的夜间时段，进入变暗状态时可能显示困倦表情；菜单里的夜静开关只控制该时段的声音，默认关闭。

### 内置书架、公网阅读与全屏叙事文字

在普通表情长按 B 后，单击 A 可在“内置书架”和“公网书源”之间切换，双击 A 进入。内置《零灯》无需 Wi-Fi；章节页中单击 A 选择下一章、单击 B 选择上一章、双击 A 开始或继续阅读，长按 B 返回来源页。当前章节、内容块与页码会保存，读完一个阅读单元后会自动进入下一章，退出或重启后可回到保存页。

KK 连接 Wi-Fi 后，从同一局域网的手机或电脑打开 `http://<设备地址>/read`。页面保留临时正文投送，同时新增“公网书源”：填入一个 `KKREAD/1` 清单的 HTTPS 地址并保存。之后在普通表情界面长按 B，按 A 进入阅读模式，KK 会主动从公网更新清单，不要求手机一直打开页面。配置页本身没有认证，只应在可信局域网使用，也不要把设备的 80 端口映射到公网；公网正文和图片则全程使用证书校验的 HTTPS。

一个清单最多包含 12 个内容块。文字块最多 1800 个 Unicode 字符和 96 个显式行，继续使用六行分页与 62 ms 打字机；图片块必须是绝对 HTTPS 地址的 JPEG 或 PNG，单张最多 768 KiB，并在黑色圆屏中等比居中显示。A 返回上一页或上一内容块，短按 B 向后继续，单击屏幕补完或继续，长按 B 满 1.5 秒退出；往回翻到内容块开头时，会跨内容块以及内置书籍章节边界，并直接打开上一文字块的最后一页。文字和图片之间先过渡到黑场，退出后再平滑睁眼恢复原表情。格式与托管示例见 [`docs/READING_SOURCES.md`](docs/READING_SOURCES.md)。

在串口监视器发送 `say <UTF-8 文字>` 即可进入叙事模式。命令保留正文原有大小写，支持中文和显式换行，串口边界最多接收 768 字节。渲染器按 Unicode 字符在圆屏中央安全区自动换行，每页最多六行，超出后自动生成后续页面。

当前表情会先让眼睛和眉毛一起收拢到黑场，随后每个字符以 62 ms 间隔逐渐亮起。内容超过一页时，底部中央以浅灰小字显示 `当前页/总页数`；单页内容不显示。一页打完后会无限停留。A 通过从右到左的扫除返回上一页；阅读内容处于第一页时，A 会进入上一内容块，内置书籍还可跨到上一章末页。短按 B 在打字时先补完整页，再次短按才通过从左到右的扫除进入下一页；单击屏幕与短按 B 相同。串口单独投送的长文没有相邻内容块，因此最后一页仍会循环回第一页。B 在 1.5 秒内松开算轻按，持续按满 1.5 秒才退出并重新睁眼，恢复进入前持续运行的表情。叙事期间屏幕保持唤醒，并暂缓充电和低电量表情，直到原表情恢复。

## 硬件

- [M5Stack StopWatch Dev Kit（C152）](https://docs.m5stack.com/en/core/StopWatch)
- ESP32-S3R8、16 MB Flash、8 MB PSRAM
- 1.75 英寸 466 × 466 圆形 AMOLED 触摸屏
- BMI270 六轴惯性传感器
- CST820B 触摸控制器
- 两个可编程按键和内置震动马达

接口、地址和当前验证边界请参阅[硬件基础资料](docs/HARDWARE_BASELINE.md)。

## 编译

需要准备：

- [PlatformIO Core](https://platformio.org/) 6.1.18
- USB-C 数据线
- M5Stack StopWatch

已经验证的依赖提交均锁定在 [`platformio.ini`](platformio.ini) 中。

```sh
pio run
```

## 烧录与串口监视

通过 USB-C 连接 StopWatch。如果没有自动开始烧录，可以按住复位键约两秒，在绿色 LED 亮起时松开。

```sh
pio run --target upload
pio device monitor --baud 115200
```

串口监视器接受 `happy`、`thinking`、`sleepy` 等表情名称，也支持以下播放测试命令：

```text
once <expression>
loop <expression>
pingpong <expression>
sound
volume 0-160
```

日常伴侣功能还支持以下串口命令：

```text
status
time
time 2026-08-26 20:00:00
brightness 150
motion low
motion medium
motion high
dim 60
screenoff 300
quiet 22 7
wifi
wifi pair
wifi retry
wifi forget
screen on
screen off
say 小谷宝贝，今天也要好好休息。
```

网络校时固定使用中国标准时间 `UTC+8`（`Asia/Shanghai`，无夏令时），联网后自动同步，失败一分钟后重试，成功后每六小时重写 RTC；串口仍可手动校时。`dim` 和 `screenoff` 的单位为秒；亮度、动作灵敏度、超时、夜间静音、安静时段、最多 5 组 Wi-Fi 凭据和公网书源地址会持久化保存。`KK-XXXX` 中的 `XXXX` 只用于区分附近设备。临时热点密码是全小写 `kkfriend`，门户最长开放五分钟，不回显或记录路由器密码；候选网络只有连接成功后才保存，失败时会继续尝试其他已保存网络。

## 仓库结构

| 路径 | 用途 |
| --- | --- |
| `src/avatar_engine.*` | 表情目录、时间轴、缓动、绘制和交互物理 |
| `src/main.cpp` | 设备初始化、触摸、IMU、按键、震动、诊断和串口命令 |
| `src/wifi_pairing.*` | 非阻塞联网、临时配网页面和凭据交接 |
| `src/reading_service.*` | 本地阅读/书源配置页和输入校验 |
| `src/public_reader.*` | 公网 HTTPS 清单、文字块和 JPEG/PNG 下载 |
| `docs/HARDWARE_BASELINE.md` | 硬件能力和验证边界 |
| `docs/ENGINEERING_NOTES.md` | 渲染实验、测量数据和实现决策 |
| `docs/ROADMAP.md` | 计划工作和暂不支持的能力 |

## 当前限制

- 麦克风和语音/大模型服务尚未接入；Wi-Fi 配网只是传输基础，串口命令仍用于模拟语义语音事件。
- 设备端仍不编辑正文；临时正文和公网书源地址需从局域网页面或外部控制入口配置。
- 公网阅读只解析 `KKREAD/1` 清单，不抓取任意网页，也不直接兼容“阅读”App/Legado 书源规则。
- 深度睡眠和外部扩展口尚未集成；当前节能策略只关闭 AMOLED，并保持输入采样以便快速唤醒。
- 网络时区目前固定为中国标准时间；如在其他时区使用，仍需增加时区设置入口。
- 尚未完成长期续航测量，默认超时只是安全起点。
- 动作与手势的主观体验可能会随设备握持方式而变化。

## 灵感与来源

本项目的表情、动画和播放分层受到 [Bible Strong Avatar Lab](https://github.com/smontlouis/bible-strong-avatar-lab) 启发。它是针对 ESP32 硬件重新实现的独立 C++ 项目，不包含上游网页应用、TypeScript 源码、导出的角色数据或视觉素材。

两者的关系可以概括为：**受到架构启发，为完全不同的硬件重新实现。**

硬件初始化、引脚映射和 IMU 屏幕坐标处理参考了 M5Stack 官方的 [StopWatch User Demo](https://github.com/m5stack/M5StopWatch-UserDemo)。详情请参阅[第三方声明](THIRD_PARTY_NOTICES.md)。

## 参与贡献

欢迎提交 Issue 和 Pull Request。修改前请阅读 [CONTRIBUTING.md](CONTRIBUTING.md)，并运行 `pio run`。涉及硬件的结论应尽可能附上真机验证证据。

## 许可证

本项目使用 [GNU Affero General Public License v3.0 or later](LICENSE) 开源。
